<div class="container">
<h5 class="text-center display-4 my-5">Cadastrar aluno</h5>

<div class="card bg-light">
	<div class="card-body mb-15">
		<form action="alunos/add" method="POST">

			<div class="form-group">
				<label for="nome">Nome:</label>
				<input type="text" id="nome" name="nome" placeholder="Nome" class="form-control" autocomplete="off" required>
			</div>

			<div class="form-group">
				<label for="turma">Turma:</label>
				<select name="turma" class="custom-select">
					{{#each turmas}}
					<option value="{{id}}">{{codigo}}</option>
					{{else}}
					<option value="0">Nenhuma turma registrada</option>
					{{/each}}
				
				</select>
			</div>

			<button type="submit" class="btn btn-success mt-4">Cadastrar Aluno</button>
			<a href="alunos/ver" class="btn btn-primary mt-4">Consultar Alunos</a>
		</form>
	</div>
</div>
</div>